// Test Case 52 — cec/kec region-sensitive chamfers
//
// This file deliberately adds a case_openings rectangle at the keyguard
// centre.  The rectangle uses cut_opening(..., "keyguard") so its top-edge
// chamfer is driven by kec (3.0 mm).  The surrounding grid cells use
// cut_opening(..., "screen") so their top-edge chamfer is driven by cec
// (0.3 mm).  The stark difference between 3.0 mm and 0.3 mm makes a
// regression easy to spot if the two values are accidentally swapped.

screen_openings = [];   // use the auto-generated grid

case_openings = [
//  ID,     x,     y, width, height, shape, top, btm, left, right, cr, other
[    1, cow/2, coh/2,    40,     30,   "r",  90,  90,   90,    90,  3,      ],
];

case_additions   = [];
tablet_openings  = [];
